@model FleetManagerWeb.Models.ClsFleetModels
@{
    ViewBag.Title = "Fleet Models";
    Layout = "~/Views/Shared/_Layout.cshtml";
 }
@using (Html.BeginForm("FleetModels", "FleetModels", FormMethod.Post, new { @id = "frmFleetModels", @class = "form_validation_ttip well stdform" }))
{

    <div class="panel panel-grey">
        <div class="panel-heading">
            @ViewBag.Title
        </div>

        <div class="panel-body pan">
            <div class="row-fluid">
                @Html.HiddenFor(m => m.hdniFrame)
                @Html.HiddenFor(m => m.lgId)
            </div>
            <div class="row form-group">
                <div class="col-md-3">
                    <div class="form-group">
                        <label class="control-label">
                            Fleet Models Name <span class="f_req">*</span>
                        </label>
                        @Html.TextBoxFor(m => m.strFleetModelsName, new { @class = "span11 required alphanumwithspc form-control", @maxlength = "20", @tabindex = "2", @next = "btnSubmit" })
                    </div>

                </div>
            </div>
            <div class="row form-group">
                <div class="col-md-12">
                    <input class="btn btn-inverse btn-primary" id="btnSubmit" name="inputSubmit" type="submit" value="Save" tabindex="3" title='Save Fleet Models' />
                    <input class="btn btn-green cancel" id="btnCancel" name="inputCancel" type="button" value="Cancel" tabindex="4" title='Cancel Fleet Models' />
                </div>
            </div>
        </div>
    </div>
}
<script src="~/Scripts/Utility/FleetModels.js"></script>
